import { Toolbar, styled } from "@mui/material"

export const StyledToolbar = styled(Toolbar)(() => {
    return (
        {
            paddingLeft: 0,
            marginBottom: "4px",
            marginTop: "4px"
        }
    )

})