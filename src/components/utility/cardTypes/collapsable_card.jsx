import { Card, CardContent, Typography } from "@mui/material";

export default function CollapsibleCard({title, content, icons}) {
    return (
        <Card>
            <CardContent>
                <Typography>HELLO</Typography>
            </CardContent>
        </Card>
    )
}